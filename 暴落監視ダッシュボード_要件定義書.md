# 暴落対応ルール監視ダッシュボード 要件定義書

**作成日：** 2026年7月
**最終更新：** 2026年9月（改修指示書 第2版の計画変更を反映）
**開発環境：** Google AntiGravity
**対象：** 新NISA攻撃フェーズ 階層型トリガー監視の自動化

---

## 1. 背景・目的

### 1.1 背景
新NISA攻撃フェーズにおいて、以下4銘柄の暴落対応ルール（階層型トリガー）を毎日目視監視する運用としている（2026-09の計画変更後の構成。FANG+は新規購入停止、代わりにTracersを新規採用）。

| 銘柄 | Tier1 | Tier2 | Tier3 |
|---|---|---|---|
| Tracers S&P500トップ10インデックス（米国株式） | -12% | -20% | -28% |
| ニッセイSOX指数インデックスファンド | -10% | -18% | -28% |
| eMAXIS Slim米国株式(S&P500) | -7% | -12% | -18% |
| eMAXIS Slim全世界株式(オルカン) | -6% | -10% | -15% |

※iFreeNEXT FANG+インデックス（Tier -15/-25/-35%）は2026-09以降新規購入停止。既存保有分の評価把握のため基準価額の表示のみ継続する。

現状は人手による毎日の確認が前提だが、**見逃しリスクの排除**と**運用負荷ゼロ化**を実現するため、自動監視ツールを自作する。

### 1.2 目的
- 毎朝ワンクリック（またはアクセスのみ）で監視対象銘柄の現状を一目で把握できること
- トリガー条件に合致した場合、即座に通知を受け取れること
- 監視自体は人手を介さず、クラウド上で自動的に毎日実行されること

---

## 2. 対象銘柄・トリガー条件（再掲）

| No | 銘柄 | 判定基準 | Tier1 | Tier2 | Tier3 |
|---|---|---|---|---|---|
| 1 | iFreeNEXT FANG+インデックス | 設定来高値比 | -15% | -25% | -35% |
| 2 | ニッセイSOX指数インデックスファンド | 設定来高値比 | -10% | -18% | -28% |
| 3 | eMAXIS Slim米国株式(S&P500) | 設定来高値比 | -7% | -12% | -18% |
| 4 | eMAXIS Slim全世界株式(オール・カントリー) | 設定来高値比 | -6% | -10% | -15% |
| 5 | Tracers S&P500トップ10インデックス（米国株式）（2026-09-19追加。ファンドコード02312245） | 設定来高値比 | -12% | -20% | -28% |

※No.1のiFreeNEXT FANG+は2026-09以降**新規購入停止**（`settings.json`の`funds[].active=false`）。基準価額の取得・ダッシュボード表示は既存保有分の評価把握のため継続するが、Tier到達の通知・LINE日次サマリー・資金投入管理（原資0円）の対象外。Tracersの判定基準日価格は2026-09-18の基準価額16,416円（`baseline.prices.tracers`、基準日は銘柄別に`baseline.dates.tracers`で個別指定）で、設定来高値もこの日を起点とする（過去履歴なし）。

### 2.1 資金配分とTier投入予定額（2026-09改訂）

攻撃フェーズの配分はTracers37%／SOX25%／S&P500 20%／オルカン18%（旧配分: FANG+15%／SOX25%／S&P500 42%／オルカン18%）。Tier投資予定額（`config/settings.json`の`funds_phase2`/`funds_phase3`）は、原資合計（②533,049円／③533,050円）をこの配分で按分し、各銘柄のTier1/2/3に25%/35%/40%ずつ割り当てる（旧配分でも同じ方式で按分されていたことを確認済み）。

| 銘柄 | 配分 | ②原資（Tier1 / 2 / 3） | ③原資（Tier1 / 2 / 3） |
|---|---|---|---|
| Tracers | 37% | 197,227円（49,307 / 69,029 / 78,891） | 197,228円（49,307 / 69,030 / 78,891） |
| SOX | 25% | 133,263円（33,316 / 46,642 / 53,305） | 同左 |
| S&P500 | 20% | 106,610円（26,652 / 37,314 / 42,644） | 同左 |
| オルカン | 18% | 95,949円（23,987 / 33,582 / 38,380） | 同左 |
| FANG+ | 0%（停止） | 0円 | 0円 |

### 2.2 出口ルール（可視化のみ）

2028年月初にSOXは、市場価格が平均取得単価を上回っていれば売却、下回っていれば最大12ヶ月（2029年月初まで）延長監視する。FANG+は2028年月初に無条件売却の予定。本システムは判定に必要な情報（銘柄別の平均取得単価と現在の基準価額の比較、F-21）の**可視化のみ**を行い、売却の判定・自動売却は行わない（スコープ外）。

### 2.3 長期ポートフォリオと2028年のリバランス（2026-09追加）

攻撃フェーズの4銘柄とは別に、別枠積立（SBI証券のつみたて投資枠の毎月積立。オルカン30,000円＋Tracers20,000円／月。旧構成のSBI・V・S&P500は2026-09に新規停止）がある。2028年月初に「攻撃フェーズ＋別枠積立を合算した長期ポートフォリオを、オルカン80%／Tracers20%へ一括リバランスする」意思決定が確定しており、その判断に**両方の評価額の合算**が必要となる。本システムは合算評価額とサテライト比率（目標20%）の可視化のみを行う（F-22・F-23。リバランスの判定・売却の自動化はスコープ外）。

**運用方針（2026-09-25ユーザー決定）**: ①FANG+・NASDAQ100・SOXは2028年に長期積立用（オルカン／Tracers）へ付け替える。②S&P500は市場動向を見て維持を判断する（判断基準は事前に決める。**基準の型は比率ベース**、数値は未確定＝残課題No.11）。③**旧つみたてNISA預りのSBI・V・S&P500はできる限り保持（売却しない）**（新規買付不可・売却しても非課税枠が復活しないため）。④リバランスの目的は分散の改善とシンプル化の両方。⑤**割合の定義（2026-09-25ユーザー指定）**: 「全体」は保有している全ファンド（SBI・V・S&P500、eMAXIS Slim全世界株式（攻撃フェーズ分＋別枠積立分）、eMAXIS Slim米国株式（S&P500）、SOX、FANG+、Tracers（Top10）、NASDAQ100）の評価額の合計（はじめてのNISAは子供用の別枠のため含めない）。**サテライト比率＝（SOX＋FANG+＋Tracers＋NASDAQ100）÷全体**、**S&P500比率＝（SBI・V・S&P500＋eMAXIS Slim S&P500）÷全体**。

**設定来高値の定義：** `config/settings.json`の`peak_start_date`（2026-07-07。購入判定基準日`baseline.date`と同一日に設定）以降に記録した各銘柄の基準価額の最高値。日次で自動更新（当日値が過去の記録より高ければ更新）。
※ピーク追跡開始日（`peak_start_date`）と、下記の資金投入期間（②期間開始日）は別の設定値であり、意図的に分離されている。ピーク追跡は2026年7月7日から開始するが、実際の資金投入は②期間（2026年8月〜）から。そのため②期間開始前でもTier到達・LINE通知は発生し得る。

**監視対象期間：**
- ②資金トリガー：2026/08〜2027/02末（原則）
- ③資金トリガー：2027/03〜2027/08末（原則）
- 両期間とも、未消化の場合は2027/12末まで延長可（例外）

---

## 3. システム構成

### 3.1 全体アーキテクチャ

```
[GitHub Actions（定時実行トリガー）]
        ↓ JST平日朝7:00頃（settings.jsonで変更可、cronはUTC・JSTの曜日ズレを補正した値を自動生成。実際の着信はGitHub Actions側の遅延により前後する）
[Pythonスクリプト：データ取得・判定]
        ↓
   ①日本経済新聞 電子版（日経電子版）投資信託ページをスクレイピング
   ②基準価額を取得・記録DBに保存
   ③設定来高値（peak_start_date以降のみ記録）と比較し下落率を計算
   ④Tier判定・購入判定（BUY/WAIT/HOLD/HIGH）
   ⑤市場心理指標（VIX・米10年金利・USD/JPY）を日経電子版から取得（F-18、Tier判定には影響しない参考情報。取得失敗時は前回値を維持）
   ⑥保有ポジション（Tier投資対象外。はじめてのNISA全世界株式は日経電子版、テスラはYahoo!ファイナンス米国株ページから取得）の含み損益を計算（F-19、Tier判定には影響しない。取得失敗時は前回値を維持）
   ⑦約定実績（`data/purchase_history.json`。運用者が購入のたびに手動追記）から銘柄別の平均取得単価を算出し、現在の基準価額と比較（F-20・F-21、表示専用で判定・通知には影響しない）
        ↓
   [判定結果・市場心理指標・保有ポジションをHTMLダッシュボードとして生成・GitHub Pagesに公開]
        ↓
   [Tier到達時のアラート通知、および平日毎朝の日次サマリー通知（保有ポジションの含み損益を含む）をLINE Messaging APIで送信（市場心理指標はLINE通知には含めない）]
        ↓
[ユーザー：スマホ/PCブラウザでダッシュボードURLにアクセス → 一目で確認]
```

### 3.2 なぜこの構成か
- **GitHub Actions採用理由：** 無料枠（public repoなら無制限、private repoでも月2,000分無料）でPC電源に依存せず定時実行できる
- **GitHub Pages採用理由：** 生成したHTMLを無料で公開でき、スマホからブックマーク1つでアクセス可能
- **LINE Notify採用理由：** メール以外の通知手段として、スマホへのプッシュ通知が最も即時性が高い

---

## 4. 機能要件

### 4.1 データ取得機能

| ID | 要件 | 詳細 |
|---|---|---|
| F-01 | 基準価額の自動取得 | 監視対象銘柄（`settings.json`の`funds`。現在5銘柄、うちFANG+は新規購入停止で表示のみ）の日次基準価額を日経電子版の投資信託ページからスクレイピングで取得 |
| F-02 | 取得失敗時のリトライ | スクレイピング失敗時は3回まで自動リトライ（間隔30秒）、それでも失敗した場合はエラー通知を送信 |
| F-03 | 取得データの永続化 | 取得した基準価額・日付をCSVまたは軽量DB（SQLite等）としてリポジトリ内に保存し、履歴を蓄積 |
| F-03b | 銘柄追加時の履歴列の自動拡張 | 監視銘柄は`settings.json`の`funds`に1エントリ追加するだけで基準価額の取得・履歴・ダッシュボードに反映される（銘柄IDのコード固定を廃止）。`history.csv`に新しい銘柄列が必要になった場合は、追記時にヘッダへ列を自動追加し既存行を空欄で補完する（列ずれを防止）。銘柄ごとに基準日が異なる場合（後から追加した銘柄）は`baseline.dates`で個別指定でき、設定来高値の起点にも使われる |
| F-04 | 設定来高値の自動更新 | 当日の基準価額が保存済みの最高値を上回った場合、来高値を自動更新。初回記録時（`peak_start_date`を過去日付に設定した場合を含む）は、`history.csv`の`peak_start_date`以降の記録も遡って確認し、本日の値より高い記録があればそちらを初期値として採用（取りこぼし防止） |

### 4.2 判定機能

| ID | 要件 | 詳細 |
|---|---|---|
| F-05 | 下落率の自動計算 | 下落率 ＝ (設定来高値 − 当日基準価額) ÷ 設定来高値。負記号「-」を用いてパーセント表示（現在値が設定来高値以上の場合は0%とし、負の下落率は発生しない） |
| F-06 | Tier判定 | 各銘柄の下落率を、Tier1/2/3の閾値と比較し、到達しているTierを判定（未到達時は「未到達」と表示） |
| F-06b | 購入判定（BUY/WAIT/HOLD/HIGH） | Tier到達に加え、現在価格が基準日価格（`settings.json`の`baseline`、許容上昇率デフォルト5%）以内かどうかで BUY／WAIT を判定。高値更新中は HIGH、Tier未到達は HOLD。基準日価格は固定値であり自動更新は行わない（現状の仕様として維持） |
| F-06c | 新規購入停止銘柄の扱い | `settings.json`の`funds[].active=false`とした銘柄（現在はFANG+）は、基準価額の取得・履歴記録・高値更新・下落率/Tier計算は継続するが、Tier到達の通知・記録（`triggered.json`）・回復判定・取得失敗通知・LINE日次サマリー・資金投入管理（F-15）の対象外とする。判定欄は「新規購入停止」（BUY等の誤解を避けるため）、ダッシュボードのカード・サマリー・チャートタブに「（新規購入停止・監視終了）」を表示する。基準日価格が未設定の銘柄は基準日比を「-」表示とする |
| F-07 | 既発動Tierの記録 | 一度発動したTierは「発動済み」として記録し、同一Tierでの重複通知を防止 |
| F-07b | 投資実行状況の記録 | 「Tier到達（通知済み）」と「実際に投資したか」を別概念として`data/triggered.json`に記録する（各記録は`{tier, date, invested, note, recovered}`形式）。デフォルトはTier到達＝投資実行（`invested: true`）だが、ユーザーが意図的に投資を見送った場合は`judge.set_investment_status()`で事後的に`invested: false`へ補正できる。資金投入管理状況（F-15）の投入済み金額は`invested: true`の記録のみを集計する |
| F-07c | 見送りTierの再通知 | 投資を見送った（`invested: false`）Tierは、そのTierの水準を一度上回るまで回復してから再びそのTierへ到達した場合にのみ「新規到達」として再通知する（`judge.update_recovery_status()`が毎日の判定時に回復状態を更新）。同じ下落局面が継続している間（回復せず同一Tier水準にとどまっている間）は再通知しない |
| F-08 | 期間内判定 | ②③のどちらの資金期間内かを日付で自動判別。開始前は「②期間開始前」、終了後は「監視期間終了」と識別（ピーク追跡自体は`peak_start_date`から別途開始しているため、②期間開始前でもTier到達・通知は発生し得る点に注意） |

### 4.3 通知機能

| ID | 要件 | 詳細 |
|---|---|---|
| F-09 | Tier到達時の即時通知 | いずれかの銘柄が新たにTierに到達した場合、当日の実行タイミングでLINE Messaging APIへ通知 |
| F-09b | 日次サマリー通知 | 平日毎朝の実行時に、Tier到達の有無にかかわらず通知対象4銘柄（新規購入停止のFANG+を除く）の状況（下落率・基準日比・判定）をまとめてLINE Messaging APIへ通知 |
| F-10 | 通知内容 | 銘柄名／到達Tier／下落率／投入予定額／実行期間（②or③）を含む |
| F-11 | データ取得エラー時の通知 | スクレイピング失敗が続いた場合、別途エラー通知を送信し「本日は確認できていない」ことを明示（新規購入停止銘柄の取得失敗は通知対象外） |

### 4.4 ダッシュボード表示機能

| ID | 要件 | 詳細 |
|---|---|---|
| F-12 | 一覧表示 | 監視対象銘柄（新規購入停止のFANG+は停止表示で併記）を1画面に並べ、各銘柄の「現在値」「設定来高値」「下落率」「Tier到達状況」を一目で確認できる |
| F-13 | 視覚的な状態表示 | 下落率に応じて色分け（緑：Tier未到達／黄：Tier1到達／橙：Tier2到達／赤：Tier3到達） |
| F-14 | 推移グラフ | 各銘柄の過去の基準価額推移と、各Tier閾値ラインを重ねた折れ線グラフを表示。Tier閾値の破線水平線は、個別銘柄タブ（`settings.json`の`funds`から動的に生成。Tracers/SOX/S&P500/オルカン/FANG+）選択時のみ表示する（「全銘柄」タブでは価格の折れ線のみとし、視認性を優先） |
| F-15 | 残存資金の表示 | （新規購入停止銘柄＝原資0円は表示対象外）②③それぞれの原資のうち、各Tierで投入済み／未投入の金額を表示 |
| F-16 | 期間表示 | 現在が②期間か③期間か、期限まで残り何日か、または②期間開始前・終了後であるかを表示 |
| F-17 | 簡易予想表示 | 直近5営業日・20営業日のトレンド（上昇/下落/横ばい）を機械的に算出し表示（複雑な予測モデルは含まない） |
| F-18 | 市場心理指標の表示 | VIX指数・米10年国債利回り・USD/JPYを日経電子版から取得し、ダッシュボード（推移チャートの直後）に参考情報として表示する。VIXは5段階（過熱/通常/警戒/恐怖/暴落級）で判定、米10年金利・USD/JPYは前回値との差分・方向を表示する。**Tier判定・BUY/WAIT判定には一切使用しない**。LINE通知には含めない（F-09/F-09b/F-10とは独立）。初心者向けに各指標の意味を説明するアコーディオンガイドを併設する |
| F-19 | 保有ポジション（監視専用）の表示 | Tier投資対象外の保有銘柄（現状: はじめてのNISA・全世界株式インデックス／テスラ）について、現在価格・取得単価・含み損益率を「資金投入管理状況」の後に別セクションとして表示する。含み損益率は5段階（絶好調/順調/様子見/注意/警戒）のしきい値判定（`config/settings.json`の`positions.gain_loss_thresholds`で変更可能）。推移チャートにも専用タブを設ける。取得単価と異なる通貨（例: テスラのUSD）は、市場心理指標（F-18）のUSD/JPYレートで円換算した参考値もあわせて表示する。取得単価は、約定実績（F-20）があれば複数回購入の平均取得単価を優先する（例: はじめてのNISA全世界株式は2回購入の平均19,514円）。**Tierベースの階層的追加投資の対象ではなく、BUY/WAIT判定ロジックにも一切影響しない**。LINE日次サマリー（F-09b）には追記するが、Tier到達アラート（F-09）には含めない |
| F-20 | 約定実績（購入実績）のグラフ表示（保有ポジションの複数回購入・平均取得単価を含む） | Tier到達時に運用者が手動でSBI証券等で買付を行った実績（約定日・約定単価・発動Tier区分・投入金額）を`data/purchase_history.json`（銘柄IDごとのレコードリスト、運用者が手動で1件ずつ追記）に記録し、該当銘柄の推移グラフ（F-14）に星マーカーとして重ね表示する。マーカーは個別銘柄タブ選択時のみ表示し、Tier区分ごとに色分け（Tier1=黄／Tier2=橙／Tier3=赤／その他=青）、ホバー時に約定日・約定単価・区分・投入金額をツールチップ表示する。保有ポジション（F-19）についても、`purchase_history.json`に銘柄IDのキーで登録された購入実績（複数回可）を、該当ポジションの推移チャートに同様のマーカーで表示し、カードに取得日・投入金額合計・購入回数を表示する。実績がある保有ポジションの取得単価（含み損益の基準）は複数回購入の平均取得単価（`settings.json`の`cost_basis`より優先）とする。実績がない場合に限り`positions.items`の`purchase_date`/`purchase_amount`（任意項目）を1件として扱う。購入日がチャートの記録済み日付と完全一致しない場合（例: ポジション追加初日など記録開始直後）は、前後7日以内で最も近い日付にスナップして表示する（マーカーのツールチップ自体は実際の約定日を表示するため、価格データを捏造するものではない）。**判定ロジック・LINE通知には一切使用しない、表示専用の手動記録データ**。既存の`data/history.csv`等の自動取得データとは完全に分離している |
| F-21 | 平均取得単価・含み損益の表示 | 攻撃フェーズ（`account`=attack、省略時）の約定実績（`data/purchase_history.json`、F-20と同一データ）から銘柄ごとの平均取得単価を算出し、現在の基準価額と並べて「投資意思決定サマリー」直後のセクションに表示する。購入口数＝投入金額÷約定単価×10,000、平均取得単価＝Σ投入金額÷Σ購入口数×10,000。現在の基準価額が平均取得単価以上なら「含み益」（緑）、下回れば「含み損」（赤）で乖離率(%)とあわせて表示。約定単価が未記録（`price: null`）の実績は計算から除外し、「約定単価が未記録の実績n件を除いた暫定値」の警告を表示する。実績が1件もない銘柄は「データなし」。SOXには`funds[].avg_cost_note`で2028年月初の出口判定（平均取得単価を上回れば売却、下回れば最大12ヶ月延長監視）の注記を表示する。**判定ロジック・LINE通知には一切使用しない、表示専用**（出口判定そのものの自動化はスコープ外） |
| F-22 | 別枠積立の約定実績記録 | `data/purchase_history.json`の実績に口座区分`account`（`attack`＝攻撃フェーズ=成長投資枠／`side`＝別枠積立=つみたて投資枠。省略時は`attack`）と保有口数`units`（省略時は投入金額÷約定単価×10,000）を任意項目として追加（後方互換）。別枠積立は攻撃フェーズと同一ファンドでも別会計として記録し、F-20のマーカー・F-21の平均取得単価には含めない。記録開始前に買った分は、口数と取得総額を1件の「既存残高」として登録する。S&P500の別枠積立はSBI・V・S&P500（別ファンド、キー`sbi_v_sp500`）で、その基準価額は`positions.items`（`hidden: true`＝保有ポジション欄・LINEには出さない）から日次取得する。SBI証券の約定履歴CSVは`scripts/import_sbi_history.py`で取り込める（預り区分から口座を自動判定、攻撃フェーズの区分は日付から自動推定＝初回購入日→①分・毎月25日→定期積立・他は未分類、重複は自動スキップ、CSVは公開リポジトリにコミットしない）。`scripts/update_purchases.py`で、取り込み→`purchase_history.json`のみコミット→リモートの日次更新を取り込んで（pull --rebase）プッシュ、までを確認（y/N）つきで1コマンド実行できる（運用者の手作業は月1回のCSVダウンロードのみ） |
| F-23 | 長期ポートフォリオの統合ビュー・サテライト比率 | 保有している全ファンド（攻撃フェーズ・別枠積立・旧つみたてNISA（`account: legacy`。保持方針））の評価額（保有口数×現在の基準価額÷10,000）を合算した「全体」と、それに対する割合を、ダッシュボードの**最下部**のセクションに表示する。**サテライト比率**＝（SOX＋FANG+＋Tracers＋NASDAQ100）÷全体を大きく表示し、目標（`long_term_portfolio.satellite_target_percent`、既定20%）との対比バー・差(pt)、計算式と金額（分子÷分母）を添える。グループ（銘柄）別のカードには評価額・全体に占める割合・口座別内訳（攻撃フェーズ／別枠積立／旧つみたてNISA）・平均取得単価を表示する。現在の基準価額が得られない銘柄がある場合は誤った値を出さず「算出不可」とする。はじめてのNISA（子供用の別枠）・テスラは含めない。NASDAQ100・SBI・V・S&P500の基準価額は`positions.items`（`hidden: true`＝保有ポジション欄・LINEには出さない）から取得する。グルーピングは`settings.json`の`long_term_portfolio`で定義（`role`はcore/satellite。銘柄IDをコードに固定しない）。**判定ロジック・LINE通知には一切使用しない表示専用**（`scripts/portfolio.py`）。既存残高が未登録の間は`provisional_note`の警告を表示する |
| F-24 | 維持判断ルール（比率ベース）の表示 | 指定したグループ（現在はS&P500）が全体に占める割合（S&P500比率）が、基準`long_term_portfolio.share_review.threshold_percent`を超えたら「見直しを検討する」というルールと現在の状況を、サテライト比率の下に表示する（現在の割合・計算式と金額・基準位置のバー・「維持（基準内）／見直し検討／基準の数値が未設定」のバッジ）。基準の数値はユーザーが事前に決めて設定ファイルに書く（未設定=null）。**表示専用で、自動売却・LINE通知は行わない**。旧つみたてNISA分は非課税枠が復活しないためできる限り保持する方針を注記する |

---

## 5. 非機能要件

| ID | 要件 | 詳細 |
|---|---|---|
| NF-01 | 運用不要 | 一度セットアップした後、人手による日次操作は一切不要（GitHub Actionsが自動実行） |
| NF-02 | 実行タイミング | 平日のみ、日本時間 朝7:00頃に自動実行（`settings.json`の`schedule`で変更可）。GitHub Actionsのschedule実行は無料枠では正確な時刻を保証されず、実際の着信は30〜60分程度遅れることがある（既知の制約として許容）。※2026-07調査により、日経電子版の基準価額は当日夜に確定・反映されるため、朝実行時点では前営業日分の確定値を取得する仕様であることが判明（不具合ではなく情報源の公表タイミングに起因する仕様）。詳細は改修履歴を参照 |
| NF-03 | 可用性 | GitHub Actions・GitHub Pagesとも無料枠内での運用を前提とし、追加費用が発生しないこと |
| NF-04 | 見やすさ | スマートフォンの小さい画面（幅320〜375px）でも横スクロールなしで全銘柄の状態が把握できるレスポンシブ対応（チャートのタブは折り返し、投資意思決定サマリーは幅600px以下で1銘柄1カードの縦積み表示、長期ポートフォリオは表ではなくカード表示。ページ全体の横スクロールを発生させない） |
| NF-05 | 取りこぼし防止 | 平日は必ず実行される設計とし、土日はスキップ（投信基準価額が更新されないため）。日付・曜日判定はすべてJST基準で行い、GitHub Actions実行環境（UTC）とのタイムゾーン差による日付ずれ・週末重複記録を防止する（祝日は未対応） |
| NF-06 | 保守性 | Tier閾値・監視対象銘柄（追加・新規購入停止`funds[].active`を含む）・原資金額・銘柄別の基準日は設定ファイル（JSON等）で管理し、コード変更なしで調整可能にする。市場心理指標（F-18）の取得元URL・VIXしきい値も同様に設定ファイルで変更可能とする |
| NF-07 | 市場心理指標の独立性 | 市場心理指標（F-18）の取得失敗はTier判定・LINE通知の動作に一切影響を与えない（例外を握りつぶして処理を継続する）。取得失敗時は前回値と最終更新日をそのまま表示し続ける |

---

## 6. 画面イメージ（ダッシュボード構成案）

```
┌─────────────────────────────────────────┐
│  🛡️ VALOR SHIELD  暴落監視              │
│  更新：2026/08/15 07:05 JST             │
│  📅 現在期間：②期間 (期限まで残197日)    │
├─────────────────────────────────────────┤
│ 💡 投資シグナルとルールの見方 (開閉ガイド) │
├─────────────────────────────────────────┤
│ 【意思決定サマリー】                     │
│  ・Tracers: -6.5%  (未到達)   ⚪ 様子見 │
│  ・SOX   : -10.1%  (Tier 1)   🟡 上昇待機│
│  ・FANG+ : -2.6%   （新規購入停止・監視終了）│
├─────────────────────────────────────────┤
│ 【平均取得単価と含み損益】               │
│  ・SOX   : 平均取得単価 48,357円         │
│            現在 43,513円  🔴 含み損 -10.0%│
│  ・S&P500: 44,914円  🔴 含み損 -2.1%     │
│  ・Tracers: データなし（購入実績未登録）  │
├─────────────────────────────────────────┤
│ 【Tracers】                           │
│  現在値: 15,300円  最高価格: 16,416円     │
│  下落率: -6.8% (Tier1まであと5.2pt)      │
│  [Tier1:-12%] [Tier2:-20%] [Tier3:-28%] │
│  (ランプ: 全て消灯 = 未到達)             │
├─────────────────────────────────────────┤
│ 【SOX】 🟡 上昇待機                     │
│  現在値: 8,900円   最高価格: 9,900円      │
│  下落率: -10.1%                         │
│  (ランプ: [Tier1]点灯 / 他は消灯)        │
│  投入済: 33,316円 ／ 残り: 79,946円       │
├─────────────────────────────────────────┤
│ [推移チャート(発光ライン)]               │
└─────────────────────────────────────────┘
```

---

## 7. 開発スコープ（Google AntiGravityへの依頼内容）

### 7.1 含めるもの
- Yahoo!ファイナンス投信ページのスクレイピングスクリプト（Python）
- 判定ロジック（下落率計算・Tier判定・期間判定）
- LINE Notify連携（Messaging APIまたはLINE Notify後継サービス）
- GitHub Actions用のワークフロー定義（YAML）
- GitHub Pages公開用のHTMLダッシュボード生成スクリプト
- 設定ファイル（銘柄コード・Tier閾値・原資金額をJSON管理）

### 7.2 含めないもの（スコープ外）
- 実際の買付注文の自動発注（SBI証券での注文は引き続き手動）
- SOXの2028年月初の出口判定（売却の判定・自動売却。平均取得単価の可視化のみ行う、2.2参照）
- 高度な株価予測AI（トレンド表示は単純な移動平均比較にとどめる）
- 複数ユーザー対応・ログイン機能（個人利用前提）

---

## 8. 残課題・確認事項

| No | 内容 | 状態 |
|---|---|---|
| 1 | データ取得元のスクレイピングが規約上問題ないか | ✅解決済み。実装では日経電子版（当初想定のYahoo!ファイナンスから変更）を個人の非営利利用・低頻度アクセス（1日1回）で利用。日経電子版側の利用規約の詳細確認は未実施（残課題） |
| 2 | 各銘柄の正確な銘柄コード | ✅解決済み。`config/settings.json` の `fund_code` に日経電子版の銘柄コードとして記録済み |
| 3 | LINE Notifyのサービス終了に伴う代替 | ✅解決済み。LINE Messaging API（Push通知）で実装済み |
| 4 | 祝日（土日以外の休場日）の扱い | 未対応。現状は土日のみ自動スキップし、祝日は通常通り実行される（休場のためスクレイピング結果が前営業日と同値になる） |
| 5 | 購入判定（BUY/WAIT）の基準日価格が固定である点 | 認識済み・現状維持の判断（2026-07レビュー時に確認）。基準日価格は自動更新されないため、長期的な相場上昇局面ではTier到達時にもWAIT判定が続く可能性がある |
| 6 | ブラインド方式による価格決定タイミングのずれ | 対応不可（制度上のルール）。対象銘柄（Tracers含む）はいずれも海外資産のため、発注日の翌営業日の海外市場終値をもとに基準価額が決定される。Tier到達判定時点の価格と実際の約定価格が乖離する可能性があり、かつ発注は当日締切後キャンセル不可。2026-08にSOX Tier2到達時、ユーザーが翌営業日の反発を見込み意図的に投資を見送った事例が発生（結果的に的中）。システム側ではF-07bで見送り判断を正しく記録できるようにし、投資ガイド（ダッシュボード）にリスクを明記する形で対応（回避策そのものは提供しない） |
| 7 | `--dry-run`テストが通知送信コードパスを通らないため、通知関数の引数不一致等が本番でしか顕在化しない | 未対応（テスト方法論上のギャップとして認識のみ）。2026-08-11、`notify_daily_summary()`の引数不一致（`monitor.py`は5引数で呼ぶが`notify.py`の定義は4引数のまま）により本番が2日連続クラッシュした事例で判明。例外が未捕捉のため通知だけでなくデータ保存・ダッシュボード生成まで巻き添えで停止した。恒久対処（`--dry-run`でも通知関数の呼び出しシグネチャを検証する等）は未実施・未合意 |
| 8 | 本番ワークフローが`data/market.json`・`data/positions.json`・`data/positions_history.csv`をコミットしていない | ✅解決済み（2026-09-19）。`.github/workflows/monitor.yml`のコミット対象が`history.csv`/`peak.json`/`triggered.json`/`index.html`のみだったため、市場心理指標の前回値・保有ポジションの最新値と日次価格履歴が実行間で引き継がれていなかった（保有ポジションの履歴は2026-08-07の1行で停止）。`git add`に3ファイルを追加して修正。欠落していた2026-08-10〜09-18分の保有ポジション価格履歴は、Yahoo!ファイナンスの時系列ページ（`finance.yahoo.co.jp/quote/<コード>/history`）から復元済み（前営業日の確定値を実行日の行に記録する既存仕様に合わせて作成、既存の2026-08-07行と一致を確認） |
| 9 | `history.csv`の欠落日（2026-07-08・07-09・08-10）の復元 | 未対応（2026-09-19時点）。日経電子版には過去日の時系列がないが、Yahoo!ファイナンスの時系列ページ（`finance.yahoo.co.jp/quote/<ファンドコード>/history`）で遡れることを保有ポジションの履歴復元で確認済み。ただし設定来高値・Tier判定への影響があるため、復元する場合は影響を確認した上でユーザーが判断する |
| 10 | 別枠積立の既存残高（2026-07以前に買った分）が未登録 | ✅解決済み（2026-09-25）。SBI証券の保有ファンドCSV（2026-09-25時点、預り区分別）から、つみたて投資枠のオルカン（既存残高14,196口・50,000円）、SBI・V・S&P500（現行つみたて投資枠 既存残高338,996口・946,682円、旧つみたてNISA 627,490口・1,093,338円）を「既存残高」レコード（基準日2026-06-30、`units`＋`amount`指定）として登録。旧つみたてNISA預りのSBI・Vも別枠積立として合算する（ユーザー判断）。登録後の口数・取得総額はSBIの保有情報と完全一致（オルカン19,495口/70,000円、SBI・V 973,868口/2,070,020円）。攻撃フェーズの登録済み実績もSBI保有情報と誤差1〜2口・数円以内で一致 |
| 11 | S&P500比率の維持判断基準 | 暫定値で設定済み（2026-09-25）。基準の型は**比率ベース**（S&P500比率＝（SBI・V＋eMAXIS Slim S&P500）÷全体が基準を超えたら「見直し検討」）、基準は**50%（暫定）**（`long_term_portfolio.share_review.threshold_percent`。S&P500の割合が高い現状から、サテライト・オルカンが今後増えていく割合の目安。現在は約88%のため「見直し検討」と表示される）。サテライト比率の目標（20%）とは別の数値。数値は投資判断のため本システムは推奨せず、ユーザーが決定・変更する |

---

## 9. 開発ステップ（想定）

1. 日経電子版から対象銘柄の基準価額を取得するスクレイピング処理を実装・動作確認
2. 下落率計算・Tier判定ロジックを実装
3. LINE Messaging API連携で通知テストを実施
4. HTMLダッシュボードのテンプレートを作成
5. GitHub Actionsワークフローを設定し、手動トリガーでの動作確認
6. 定時実行（cron）を設定し、数日間の自動実行をテスト
7. 本番運用開始

---

## 10. 改修履歴

| 時期 | 内容 |
|---|---|
| 2026-07 | Claude Codeへ開発移管。コードレビューにより以下の不具合を発見・修正：①`date.today()`がUTC基準で日付・曜日判定を行っており、JST実行時刻との間で日付が1日ずれる不具合、②`schedule.daily`が`true`のまま運用されており土日も実行され、休場日のNAVが別日付で重複記録される不具合、③監視開始前（`peak_start_date`未到達）にもかかわらず設定来高値が仮登録され、その後NAVがそれを上回ると下落率が負値になりLINE通知が「--1.1%」のような二重マイナス表記になる不具合。いずれも修正済み（[scripts/monitor.py](scripts/monitor.py), [scripts/judge.py](scripts/judge.py)）。あわせて`.gitignore`整備、判定表示ロジックの共通化、ダッシュボードの表記修正（データ出典・HIGH配色）を実施。GitHub Actions本番実行によりLINE通知・ダッシュボード反映まで動作確認済み |
| 2026-07 | `peak_start_date`を`2026-08-01`（②期間開始日）から`2026-07-07`（購入判定基準日`baseline.date`と同一日）に変更。ユーザー判断により、②期間開始前から高値追跡を行う運用に変更。`peak_start_date`を過去日付に設定してもhistory.csvの記録済み履歴を遡って確認し、取りこぼしなく初期ピークを設定する処理を追加（`judge.seed_initial_peak()`） |
| 2026-07 | `history.csv`で3日連続同値が記録された事象をユーザーから指摘され調査。日経電子版の基準価額は当日夜に確定・反映されるため、朝実行では前営業日分の確定値を取得する仕様であることが判明（NF-02参照）。スクレイパー自体の不具合ではないことをライブ再取得・ページ内「前日比」計算との整合確認により検証。土日分の重複は既存のweekdayガードで解消済みのため実害は限定的と判断し、現状の朝実行方式を維持する方針とした |
| 2026-07 | 実行時刻をJST 07:00から07:15に変更（ユーザーの私的な確認しやすい時刻に合わせた設定変更） |
| 2026-07 | 朝の本番実行結果をユーザーが確認したところ、①GitHub Actionsのschedule実行が予定時刻から55分遅延（08:10着信）、②オルカンが基準日価格を下回ったままなのに「本日高値更新（HIGH）」と誤判定、という2点が判明。①はGitHub側の既知の仕様のため実行時刻を07:15→07:00に戻し遅延を吸収する運用とした。②は`update_peak()`が`history.csv`の欠落期間（2026-07-07〜09、監視ツール導入前で未記録）を考慮しておらず、`settings.json`の`baseline.prices`（基準日実績値）を見逃していたことが原因と判明。基準日価格を来高値の下限として毎回確認する自己修復ロジックを追加し修正。あわせてLINE日次サマリーの「最高値比」「基準日比」を改行表示に変更、期間ラベル「監視開始前」を「②期間開始前」に変更しF-08/F-16の記述も追従 |
| 2026-07 | 市場心理インジケータ（F-18）を追加。ユーザーから提示された改修指示書（VIX/CNN Fear & Greed Index/信用評価損益率/米10年金利/USD-JPYの5指標案）を実現可能性調査の上で評価：CNN Fear & Greed Indexは非公式APIのみでCNN側の変更リスクが高いため、信用評価損益率は週次更新かつ対象4銘柄（すべて海外株式）との連動性が薄いため、いずれも不採用と判断。VIX・米10年金利・USD/JPYの3指標を採用し、日経電子版の`marketdata/quote`ページ（`[class*="IndicatorSummary_value"]`セレクタ）から取得する`scripts/market_data.py`を新設。ユーザー指示により**LINE通知には含めず、ダッシュボードのみに参考情報として表示**（BUY/WAIT判定への影響なし）。初心者向けに各指標の意味を説明するアコーディオンガイドを併設し、取得失敗時は`data/market.json`の前回値にフォールバックする自己修復設計とした |
| 2026-08 | 2026-07-31にSOXがTier2到達したが、ユーザーが翌営業日の株価上昇を見込み意図的に投資を見送った（結果的に的中）ことを受け、①ブラインド方式による価格決定タイミングのずれ（残課題No.6）、②`data/triggered.json`が「Tier到達＝投資済み」を前提にしており見送りを正しく扱えない設計上の欠陥、の2点を調査・対応。`data/triggered.json`のスキーマを`{tier, date, invested, note}`形式のレコードに拡張（旧形式の整数リストは`judge.load_triggered()`が自動移行）し、「到達（通知済み）」と「実際に投資したか」を分離（F-07b）。`judge.set_investment_status()`を新設し、SOX Tier2を`invested: false`へ補正（見送っても同一Tierへの重複通知は発生しない設計）。ダッシュボードの資金投入管理状況に見送り注記を追加し、投資ガイドにブラインド方式のリスクを明記 |
| 2026-08 | 上記の対応後、ユーザーから「見送ったTierが再度到達した場合は通知してほしい」との追加要望。見送り後ずっと再通知されない設計では機会損失になるため、`recovered`フィールドを`data/triggered.json`のレコードに追加（F-07c）。`judge.update_recovery_status()`を新設し、見送ったTierの水準を一度でも上回れば`recovered: true`とマークする。`is_new_trigger()`を、`invested: false`かつ`recovered: true`の場合のみ再通知対象と判定するよう修正。再通知の頻度（毎日 vs 回復後の再到達時のみ）をユーザーに確認し、「回復してから再度下落した時のみ」を採用（同一下落局面での毎日通知による頻度過多を回避） |
| 2026-08 | 保有ポジション（監視専用）機能を追加（F-19）。ユーザーが新規購入した「はじめてのNISA・全世界株式インデックス（オール・カントリー）」（既存の「オルカン」＝eMAXIS Slim全世界株式とは別ファンド、ファンドコード01312237）と「テスラ」（米国株）について、ヒアリングにより①既存4銘柄のTierベース階層投資とは別物とし監視・通知のみとする、②含み損益に5段階の警戒レベルを設ける、③ダッシュボードは既存の投資意思決定サマリーとは別セクションで分離、④LINE日次サマリーに追記、で方針を確定。米国株式市場も日本と同様に土日休場のため既存の土日スキップガードの対象内で実行することも確認済み。新規`scripts/positions.py`をTier系（`judge.py`/`fetch_nav.py`）から完全に分離した独立モジュールとして実装し、`data/history.csv`等の既存データファイルには一切変更を加えない設計とした。日経電子版に米国個別株ページがないため、テスラはYahoo!ファイナンス（`finance.yahoo.co.jp/quote/TSLA`、15分ディレイ）から取得。テスラの円換算には市場心理指標（F-18）のUSD/JPYレートを流用 |
| 2026-08 | 上記の保有ポジション機能追加時の実装漏れにより、`notify.py`の`notify_daily_summary()`関数定義が`monitor.py`側の呼び出し（5引数）に追従しておらず（4引数のまま）、2026-08-10・08-11の本番実行が`TypeError`で2日連続クラッシュ。例外が未捕捉だったため通知だけでなくpeak.json/triggered.json保存・history.csv追記・ダッシュボード生成まで巻き添えで停止していた。`gh run view --log-failed`で本番ログから原因を特定し、`notify_daily_summary()`に`positions_display`引数を追加して修正（コミット`436b88c`）。両日ともTier=0（HOLD/HIGH）のみでBUYシグナルの見逃しはなし。2026-08-10分のデータは日経電子版に過去日の時系列データがなく恒久的に復旧不可能、2026-08-11分は本番相当の再実行（`workflow_dispatch`, dry_run=false）で復旧を確認。`--dry-run`が通知コードパスを通らずローカルで検出できなかった点をテスト方法論上の課題として残課題No.7に記録 |
| 2026-09 | ユーザー提供の改修指示書に基づき2点対応。①「はじめてのNISA全世界株式」の約定日修正を依頼されたが、調査の結果そもそも約定日を記録・表示する機能自体が存在しない（ダッシュボードの唯一の日付表示は価格取得日であり購入日ではない）ことが判明し、ユーザーに確認の上、`positions.items`に`purchase_date`/`purchase_amount`（任意項目）を新設して対応（F-19拡張）。②Tier到達時の手動購入実績をグラフにマーカー表示する機能（F-20）を新設、新規`data/purchase_history.json`（銘柄IDごとの手動記録リスト）と`scripts/purchase_history.py`を追加。あわせて、調査中に「基準価額の推移とTier閾値」グラフ（F-14）がTier閾値の水平線を実際には描画していない（`tier_lines`データを生成しているがJS側で未使用）という別件の不具合を発見し、ユーザー確認の上同時に修正。マーカーは個別銘柄タブ選択時のみ表示し、購入日がチャートの記録開始日直後などで完全一致しない場合は前後7日以内の最近傍日にスナップする自己修復ロジックを追加（データ捏造は行わず、ツールチップには実際の約定日を表示） |
| 2026-09 | 攻撃フェーズの計画変更（改修指示書 第2版）に対応。①監視銘柄の更新：Tracers S&P500トップ10インデックス（ファンドコード02312245、Tier -12/-20/-28%、日経電子版の既存セレクタで取得可能なことを実地確認）を追加し、iFreeNEXT FANG+は新規購入停止（`funds[].active=false`）としてTier通知・LINE日次サマリー・資金投入管理の対象外にした（基準価額の取得とダッシュボード表示は継続、「新規購入停止」表示）。判定基準日価格が未設定の銘柄は基準日比を「-」表示（銘柄ごとの基準日は`baseline.dates`で個別指定可能）。Tier投資予定額は現行原資（②533,049円／③533,050円）を旧配分15/25/42/18%で按分した値と一致することを確認の上、新配分（Tracers37%／SOX25%／S&P500 20%／オルカン18%、各銘柄のTier比率25/35/40%）で再計算。銘柄ID4件固定だった`history.csv`（銘柄追加時にヘッダ列を自動拡張し既存行を空欄補完）・`triggered.json`・`purchase_history.json`のデフォルト、ダッシュボードのチャートタブを設定駆動に修正。②平均取得単価の算出・表示（F-21）を新設。SOXの2028年月初出口判定用の可視化のみで、判定の自動化はスコープ外。約定単価未記録（null）の実績は計算から除外して警告表示する安全策を追加 |
| 2026-09 | 上記の対応後、はじめてのNISA全世界株式（保有ポジション）が2026-09-14に2回目の購入（75,000円@19,220円）をしていたことが約定履歴で判明したため、ユーザー確認の上、保有ポジションの取得単価を約定実績の平均取得単価（19,514円）に切り替え、カードの取得日・投入金額を複数回分に、推移チャートのマーカーを購入ごとに表示するよう拡張（`purchase_history.resolve_cost_basis()`/`position_purchases()`）。調査中、本番ワークフローがmarket.json/positions.json/positions_history.csvをコミットしておらず保有ポジション・市場心理の履歴が引き継がれていないことを発見し残課題No.8に記録 |
| 2026-09 | 本番ワークフローのコミット対象に`data/market.json`・`data/positions.json`・`data/positions_history.csv`を追加（残課題No.8）。保有ポジション・市場心理機能の追加時（2026-07/08）から、これらの状態ファイルが実行間で保存されていなかった。ユーザー承認の上でCI設定を変更 |
| 2026-09 | 上記No.8で欠落していた保有ポジションの価格履歴（2026-08-10〜09-18）を、Yahoo!ファイナンスの時系列（はじめてのNISA=基準価額、テスラ=終値）から復元。日経電子版には過去日の時系列がないが、Yahoo!ファイナンスには公開されていることを確認（`history.csv`の欠落日〜7/8・7/9・8/10〜も同様に復元できる可能性があるが、ピーク判定への影響があるため未実施） |
| 2026-09 | 改修指示書 第3版に対応（別枠積立の約定実績記録・統合ビュー）。別枠積立＝つみたて投資枠の毎月積立と確認（企業型DCではない）。①`purchase_history.json`に任意項目`account`（attack/side）・`units`を追加（後方互換）し、既存のオルカンつみたて枠2件を別枠積立へ分離、SBI・V・S&P500（別ファンド、fcode 89311199）の3件を登録。②新規`scripts/portfolio.py`で攻撃フェーズ＋別枠積立の合計評価額・サテライト比率（目標20%）を算出し、ダッシュボードに統合ビューを追加（F-22・F-23）。はじめてのNISAは合計に含めない。③SBI・Vの基準価額は既存の`positions`機構（`hidden: true`）を再利用し、その際に`positions.py`の`append_positions_history()`にヘッダ移行処理がなく銘柄追加で列ずれする潜在不具合を発見・修正。④SBI証券の約定履歴CSV取り込みスクリプト`scripts/import_sbi_history.py`を追加。⑤別枠積立の既存残高は未登録のため暫定値表示（残課題No.10）。実装時期は低〜中優先度の指示だったが、データ蓄積のため先行して実装 |
| 2026-09 | 長期ポートフォリオの割合の定義を変更（ユーザー指定）。従来の「(Tracers＋S&P500)÷(オルカン＋Tracers＋S&P500)」→「全体＝保有全ファンドの評価額合計」に対し、サテライト比率＝（SOX＋FANG+＋Tracers＋NASDAQ100）÷全体、S&P500比率＝（SBI・V＋eMAXIS Slim S&P500）÷全体。`hold`ロール（リバランス対象外）を廃止して単純化し、維持判断ルールを`share_review`に改名。NASDAQ100（fcode 29313233）を購入実績（SBI約定履歴＋保有CSVとの差を既存残高として登録、SBI保有情報と一致）・基準価額取得（`hidden`ポジション）に追加。ダッシュボードは計算式と金額（分子÷分母）を併記して分かりやすく表示。あわせてスマホの横スクロール解消（チャートタブの折り返し、サマリーのカード縦積み）、長期ポートフォリオを最下部へ移動、旧つみたてNISAを`legacy`口座として保持扱い、S&P500の維持判断ルール（比率ベース）を追加 |
| 2026-09 | 上記の割合の定義を確定（ユーザー確認）：オルカンの2回記載は攻撃フェーズ分＋別枠積立分（月3万円）。ニッセイNASDAQ100は子供用の別枠のため長期ポートフォリオ（全体・サテライト比率）から除外（購入実績・基準価額取得も削除）。S&P500比率の基準を50%（暫定）に設定 |
| 2026-09 | 上記の訂正（ユーザーからの誤記訂正）：子供用の別枠で計算に含めないのは「はじめてのNISA・全世界株式インデックス」であり、ニッセイNASDAQ100ではなかった。NASDAQ100は長期ポートフォリオ（全体・サテライト比率）に含める定義に戻し、購入実績・基準価額取得を復元。S&P500比率の基準50%（暫定）はそのまま |
| 2026-09 | 購入実績の更新の運用負荷を下げるため（ユーザー要望「平均取得単価などを都度Claudeに渡す必要があるのか」）、①CSV取り込みで攻撃フェーズの区分を日付から自動推定（`sbi_import.attack_category_rules`）、②取り込み→コミット→pull --rebase→プッシュを確認つきで1コマンド実行する`scripts/update_purchases.py`を追加。平均取得単価・評価額・比率は購入実績から自動計算されるため、運用者は約定履歴CSVをダウンロードして実行するだけで、Claudeへの情報提供は不要 |
